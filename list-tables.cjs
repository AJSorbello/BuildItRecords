const { Pool } = require("pg"); const pool = new Pool({ connectionString: "postgres://postgres:postgres@liuaozuvkmvanmchndzl.supabase.co:5432/postgres?sslmode=require", ssl: { rejectUnauthorized: false } }); async function main() { const client = await pool.connect(); try { console.log("Listing all tables in the database"); const tables = await client.query("SELECT table_name FROM information_schema.tables WHERE table_schema = public ORDER BY table_name"); console.log("Tables found:", tables.rows.length); tables.rows.forEach(row => console.log(`  - ${row.table_name}`)); } finally { client.release(); } } main().catch(console.error);
