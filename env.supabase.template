# Supabase configuration template
# Copy this file to .env.supabase and fill in your values

# Supabase URL (required)
SUPABASE_URL=https://your-project-id.supabase.co

# Authentication - Choose one of these methods:
# 1. Service Role Key (preferred for bypassing RLS)
SUPABASE_SERVICE_KEY=your-service-role-key

# 2. Or User credentials (if service key not available)
SUPABASE_USER=your-admin-email@example.com
SUPABASE_PASSWORD=your-admin-password

# Only use this if neither service key nor user credentials are available
# Not recommended as it won't bypass RLS policies
# SUPABASE_ANON_KEY=your-anon-key
